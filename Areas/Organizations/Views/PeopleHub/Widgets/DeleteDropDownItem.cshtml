@model Guid?
@using (Html.BeginForm("Delete", "PeopleHub", new {id = Model}, FormMethod.Post))
{
  @Html.AntiForgeryToken()
  <button type="submit" class="btn btn-link btn-dropdown">Delete</button>
}
