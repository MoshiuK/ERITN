@model IReadOnlyCollection<AccessManagement.Web.Areas.Organizations.ViewModels.PeopleHub.ActiveCredentialViewModel>
@{
  foreach (var c in Model.OrderBy(o => o.Event.Name))
  {
    <div class="col-md-12">
      @c.AccessLevel.LevelMoniker @c.Event.Name
    </div>
  }
}