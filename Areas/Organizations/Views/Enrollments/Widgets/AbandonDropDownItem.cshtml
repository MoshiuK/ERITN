@model Guid?
@using (Html.BeginForm("AbandonInvitation", "Enrollments", new { id = Model }, FormMethod.Post))
{
  @Html.AntiForgeryToken()
  <button type="submit" class="btn btn-link btn-dropdown">Abandon</button>
}