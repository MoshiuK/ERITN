@using System.Web.Mvc.Html
@model AccessManagement.Web.Areas.Administration.ViewModels.AddAccessLevelDialogViewModel
<div id="@Model.DialogId" class="modal fade" tabindex="-1" role="dialog">
  <div class="modal-dialog" role="document">
    @using (Html.BeginForm("AddAccessLevel", "Settings", FormMethod.Post))
    {
      @Html.AntiForgeryToken()

      <div class="modal-content">
        <div class="modal-header">
          <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
          <h4 class="modal-title">Add an Access Level</h4>
        </div>
        <div class="modal-body" style="min-height: 200px">
          <p>The following access levels are available in the sytem. Click <strong>Add</strong> to make this access level available to organizations in your program.
          </p>
          <div class="row">
            <div class="col-md-12">
              <div class="form-group">
                @Html.LabelFor(m => m.AccessLevel)
                @Html.DropDownListFor(m => m.AccessLevel, Model.SelectAccessLevels, new { @class = "select2-component form-control" })
              </div>
            </div>
          </div>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-default" data-dismiss="modal">Cancel</button>
          <button type="submit" class="btn btn-primary">Add</button>
        </div>
      </div>
    }
  </div>
</div>
