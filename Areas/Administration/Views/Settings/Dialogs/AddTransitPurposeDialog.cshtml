@using System.Web.Mvc.Html
@model AccessManagement.Web.Areas.Administration.ViewModels.AddTransitPurposeDialogViewModel
<div id="@Model.DialogId" class="modal fade" tabindex="-1" role="dialog">
  <div class="modal-dialog" role="document">
    @using (Html.BeginForm("AddTransitPurpose", "Settings", FormMethod.Post))
    {
      @Html.AntiForgeryToken()

      <div class="modal-content">
        <div class="modal-header">
          <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
          <h4 class="modal-title">Add a Transit Purpose / Support Function</h4>
        </div>
        <div class="modal-body" style="min-height: 200px">
          <p>The following transit purposes / support functions are available in the sytem. Click <strong>Add</strong> to make this purpose / support function available to registering organizations.
          </p>
          <div class="row">
            <div class="col-md-12">
              <div class="form-group">
                @Html.LabelFor(m => m.TransitPurpose)
                @Html.DropDownListFor(m => m.TransitPurpose, Model.SelectTransitPurposes, new { @class = "select2-component form-control" })
              </div>
            </div>
          </div>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-default" data-dismiss="modal">Cancel</button>
          <button type="submit" class="btn btn-primary">Add</button>
        </div>
      </div>
    }
  </div>
</div>
