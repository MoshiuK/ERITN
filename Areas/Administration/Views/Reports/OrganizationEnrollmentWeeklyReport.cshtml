@model IReadOnlyCollection<AccessManagement.Web.ViewModels.Framework.Reports.ReportDataRow>

@{
  Layout = "../Shared/_HubLayout.cshtml";

  ViewBag.ColumnCount = 6;
}

<h2>@ViewBag.Title</h2>
<small>Found @Model.Count report rows.</small>

<div class="table-scroll-container">
  <table class="table table-condensed table-bordered">
    <thead>
      <tr>
        <th class="md-l">Organization Name</th>
        <th class="md-l">New Self Enrollments</th>
        <th class="md-l">Other Enrollments</th>
        <th class="md-l">Total New Enrollments</th>
        <th class="md-l">New Orders this Week</th>
        <th class="md-l">Date of Last Enrollment</th>
      </tr>
    </thead>
    <tbody>
      @foreach (var row in Model)
      {
        @Html.DisplayFor(_ => row.Data, row.DataTemplate);
      }
    </tbody>
  </table>
</div>
