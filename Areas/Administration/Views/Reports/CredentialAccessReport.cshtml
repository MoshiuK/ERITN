@model IReadOnlyCollection<AccessManagement.Web.ViewModels.Framework.Reports.ReportDataRow>
@{
  Layout = "../Shared/_HubLayout.cshtml";

  ViewBag.ColumnCount = 7;
}

<h2>@ViewBag.Title</h2>
<small>Found @Model.Count report rows.</small>

<div class="table-scroll-container">
  <table class="table table-condensed table-bordered">
    <thead>
      <tr>
        <th class="md-l">Event Type</th>
        <th class="md-l">Occurred</th>
        <th class="lg-l">Member</th>
        <th class="sm-c">Access Level</th>
        <th class="sm-c">Credential Ref#</th>
        <th class="md-l">Location</th>
        <th class="lg-l">Remarks</th>
      </tr>
    </thead>
    <tbody>
    @foreach (var row in Model)
    {
      @Html.DisplayFor(_ => row.Data, row.DataTemplate);
    }
    </tbody>
  </table>
</div>