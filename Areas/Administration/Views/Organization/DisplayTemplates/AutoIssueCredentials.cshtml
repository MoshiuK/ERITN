@using Juillet.Web.Mvc.Html
@model bool
@if (Model)
{
  <div data-toggle="popover"
       data-placement="left"
       data-trigger="hover"
       data-content="Automatically issue credentials when events in this location are activated.">
    @Html.GlyphFa("id-badge", GlyphStyles.FixedWidth) <small>Issue credentials automatically</small>
  </div>
}
