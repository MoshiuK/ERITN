@using Juillet.Web.Mvc.Html
@model bool
@if (Model)
{
  <div data-toggle="popover"
       data-placement="left"
       data-trigger="hover"
       data-content="Automatically distribute credentials to personnel.">
    @Html.GlyphFa("envelope", GlyphStyles.FixedWidth) <small>Distribute credentials automatically</small>
  </div>
}
