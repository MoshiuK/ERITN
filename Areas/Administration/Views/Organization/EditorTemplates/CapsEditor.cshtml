@model MultiSelectList

<ul class="tag-list">
  @foreach (var tag in Model)
  {
    var css = tag.Selected ? "active" : "";
    <li class="@css" data-sps-action="toggle-cap" data-cap-value="@tag.Text">@tag.Text</li>
  }
</ul>
