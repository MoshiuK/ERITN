@model AccessManagement.Web.Areas.Operations.ViewModels.System.NewTransitLocation

<div class="row">
  <div class="col-md-offset-3 col-md-6">
    <div class="panel">
      <div class="panel-body">
        @using (Html.BeginForm("NewTransitLocation"))
        {
          @Html.AntiForgeryToken()

          @Html.EditorForModel()

          @Html.ActionLink("Cancel", "TransitLocations", null, new {@class = "btn btn-default"})
          <button type="submit" class="btn btn-primary">Create</button>
        }
      </div>
    </div>
  </div>
</div>
